# Curso: Ingeniería de Software II

# Estudiante:  Ángel David Gómez Pastrana

# Laboratorio REST y GraphQL con Postman

# Descripción de la rama

**Contenido:**

- ``/GraphQL:``
  - ``/GraphQL.md:`` Documento del laboratorio GraphQL: uso de la API Countries (Trevor Blades), descripción de 5 requests de Postman, resultados y comparativa con REST.
  - ``/Env_GraphQL_Tarea.postman_environment.json:`` Archivo de entorno de Postman para GraphQL (variables usadas en las requests).
  - ``/Tarea GraphQL - Angel Gomez.json:`` Json con el cuerpo de los requests GraphQL y scrpits de test after response.
  - ``/images/:`` Capturas `R{i}_Test` y `R{i}_Response` con respuestas y resultados de tests para las queries de GraphQL.

- ``/REST:``
  - ``/REST.md:`` Documento del laboratorio REST: APIs usadas (CoinGecko y NASA APOD), endpoints consultados, uso de API key/DEMO_KEY, códigos de respuesta y capturas.
  - ``/Env_tarea_REST.postman_environment.json:`` Archivo de entorno de Postman para REST (variables como `api_key`, URLs y configuración).
  - ``/Tarea Postman.postman_collection.json:`` Colección de Postman con los requests REST y tests automatizados.
  - ``/images/:`` Capturas `R{i}_Test` y `R{i}_Response` que ilustran responses y tests para los requests REST.


